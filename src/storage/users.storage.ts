export type User = {
    name: string,
    last_name: string
}

export const users: User[] = []

export function addUser(name: string, last_name: string) {
    let user: User = {
        // id: string, 
        name: string,
        last_name: string
    }
}